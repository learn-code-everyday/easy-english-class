services:
  app:
    image: beenguyen0601/english-class-fe:prod
    container_name: english-class-fe
    restart: unless-stopped

    env_file:
      - .env

    networks:
      - app-net

    ports:
      - "4000:3000"

networks:
  app-net:
    external: true